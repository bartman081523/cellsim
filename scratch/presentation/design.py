"""Gemeinsame Design-Konstanten (SciComPresentationMind: ein Farbsystem
für das ganze Medium — Manim-Szenen UND PPTX-Deck)."""

BG = "#0E1116"        # Hintergrund
INK = "#E6E9EF"       # Text
BLUE = "#4FC3F7"      # Phänomen / Messwerte
ORANGE = "#F2994A"    # registrierte Schwellen / Kanten
GREEN = "#66BB6A"     # bestandene Kriterien
RED = "#EF5350"       # falsifiziert / inert (gleiche Würde wie Erfolg)
GREY = "#3A4150"      # Kontrolle / Träger
GREY_LIGHT = "#8A93A6"  # Sekundärtext

W, H = 1920, 1080     # Render-Auflösung
FPS = 30

# Schriftgrößen (pptx in pt, manim in font_size px)
T_SIZE = {"title": 40, "head": 30, "body": 20, "small": 15, "tiny": 12}
